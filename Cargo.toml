[package]
name = "admrule-kr-compiler"
version = "0.0.0"
edition = "2024"
description = "Compile cached admrule XML into a bare Git repository (admrule-kr)"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }

[profile.release]
lto = "fat"
codegen-units = 1
